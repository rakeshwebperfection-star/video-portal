"use client";

import { motion } from "framer-motion";
import {
  BadgeCheck,
  Bell,
  Bot,
  Check,
  CreditCard,
  FileQuestion,
  Film,
  Globe,
  ImageIcon,  Layers,
  LifeBuoy,  Lock,
  Mail,
  MapPin,
  MessageCircle,
  Phone,
  RefreshCw,
  Save,
  Send,
  Settings,
  Shield,
  Sparkles,  Upload,
  Wand2,  Zap,
} from "lucide-react";
import * as React from "react";
import { FieldLabel } from "@/components/common/FieldLabel";
import { GlassPanel } from "@/components/common/GlassPanel";
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Slider } from "@/components/ui/slider";
import { Textarea } from "@/components/ui/textarea";
import { useToast } from "@/hooks/use-toast";

function PageHeader({ eyebrow, title, description, icon: Icon }: { eyebrow: string; title: string; description: string; icon: React.ElementType }) {
  return <div className="mb-5 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-medium text-sky-300">{eyebrow}</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-white">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">{description}</p></div><div className="grid h-14 w-14 place-items-center rounded-2xl border border-violet-300/30 bg-violet-500/15 text-violet-200 shadow-[0_0_30px_rgba(124,58,237,0.25)]"><Icon className="h-7 w-7" /></div></div>;
}

function MiniStat({ label, value, icon: Icon }: { label: string; value: string; icon: React.ElementType }) {
  return <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><div className="mb-4 flex items-center justify-between"><span className="text-sm text-slate-400">{label}</span><Icon className="h-5 w-5 text-sky-300" /></div><p className="text-2xl font-bold text-white">{value}</p></div>;
}

function ToggleRow({ label, checked, onChange }: { label: string; checked: boolean; onChange: () => void }) {
  return <button onClick={onChange} className="flex w-full items-center justify-between rounded-2xl border border-white/10 bg-white/[0.04] p-4 text-left text-sm text-white"><span>{label}</span><span className={`h-6 w-11 rounded-full p-1 transition ${checked ? "bg-violet-500" : "bg-white/10"}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${checked ? "translate-x-5" : ""}`} /></span></button>;
}

export function HomePage({ onNavigate }: { onNavigate: (item: string) => void }) {
  return <div className="space-y-4"><GlassPanel className="p-6 sm:p-8"><PageHeader eyebrow="Command Center" title="Welcome back to Vidora AI" description="Generate videos, manage creations, launch tools, and monitor your creative workspace from one cinematic dashboard." icon={Bot} /><div className="grid gap-3 md:grid-cols-4"><MiniStat label="Credits" value="120" icon={Zap} /><MiniStat label="Videos" value="48" icon={Film} /><MiniStat label="Templates" value="25" icon={Layers} /><MiniStat label="Plan" value="Pro" icon={BadgeCheck} /></div><div className="mt-5 flex flex-wrap gap-3">{[["Generate Video", Sparkles], ["Image to Video", ImageIcon], ["Templates", Layers], ["Billing", CreditCard]].map(([label, Icon]) => <Button key={label as string} variant="outline" onClick={() => onNavigate(label as string)}>{React.createElement(Icon as React.ElementType, { className: "h-4 w-4" })}{label as string}</Button>)}</div></GlassPanel></div>;
}

export function MediaToVideoPage({ mode, onSendToGenerator }: { mode: "image" | "text"; onSendToGenerator: (prompt: string) => void }) {
  const { toast } = useToast();
  const [source, setSource] = React.useState("");
  const [prompt, setPrompt] = React.useState(mode === "image" ? "Animate this scene with cinematic parallax, soft camera movement, and neon highlights." : "A cinematic opening shot of a futuristic explorer entering a glowing city.");
  const [progress, setProgress] = React.useState(0);
  const [isRunning, setIsRunning] = React.useState(false);
  const runWorkflow = () => { setIsRunning(true); setProgress(0); const interval = window.setInterval(() => { setProgress((current) => { const next = Math.min(current + 10, 100); if (next === 100) { window.clearInterval(interval); window.setTimeout(() => { setIsRunning(false); toast({ title: "Workflow ready", description: "Prompt has been prepared for video generation." }); }, 250); } return next; }); }, 140); };
  return <GlassPanel className="p-6 sm:p-8"><PageHeader eyebrow={mode === "image" ? "Image Workflow" : "Text Workflow"} title={mode === "image" ? "Image to Video" : "Text to Video"} description={mode === "image" ? "Upload or reference an image, define motion, and send it into the generator." : "Write a story idea, tune style, and generate a production-ready video prompt."} icon={mode === "image" ? ImageIcon : Film} /><div className="grid gap-4 xl:grid-cols-[0.9fr_1.1fr]"><div className="rounded-2xl border border-dashed border-violet-300/30 bg-white/[0.035] p-5"><div className="grid min-h-64 place-items-center rounded-2xl border border-white/10 bg-[#06070A]/70 p-6 text-center"><Upload className="mx-auto h-10 w-10 text-violet-300" /><h3 className="mt-4 text-lg font-semibold text-white">{mode === "image" ? "Upload reference image" : "Add script source"}</h3><p className="mt-2 text-sm text-slate-400">{mode === "image" ? "Paste an image URL or use this uploader mock." : "Paste source notes, scene beats, or a product story."}</p><Input className="mt-5" value={source} onChange={(event) => setSource(event.target.value)} placeholder={mode === "image" ? "https://image-url.com/frame.png" : "Scene notes or campaign source"} /></div></div><div className="space-y-4"><div><FieldLabel label="Creative Prompt" value={`${prompt.length}/800`} /><Textarea value={prompt} maxLength={800} onChange={(event) => setPrompt(event.target.value)} className="min-h-36" /></div><div className="grid gap-4 sm:grid-cols-3"><div><FieldLabel label="Motion" /><Select defaultValue="cinematic"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="cinematic">Cinematic</SelectItem><SelectItem value="smooth">Smooth</SelectItem><SelectItem value="dynamic">Dynamic</SelectItem></SelectContent></Select></div><div><FieldLabel label="Style" /><Select defaultValue="realistic"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="realistic">Realistic</SelectItem><SelectItem value="anime">Anime</SelectItem><SelectItem value="3d">Hyper 3D</SelectItem></SelectContent></Select></div><div><FieldLabel label="Output" /><Select defaultValue="1080p"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="720p">720p</SelectItem><SelectItem value="1080p">1080p</SelectItem><SelectItem value="4k">4K</SelectItem></SelectContent></Select></div></div>{isRunning ? <div className="rounded-2xl border border-sky-300/20 bg-sky-400/5 p-4"><div className="mb-2 flex justify-between text-sm text-sky-100"><span>Preparing workflow</span><span>{progress}%</span></div><div className="h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-sky-400 to-violet-500" style={{ width: `${progress}%` }} /></div></div> : null}<div className="flex flex-wrap gap-3"><Button variant="gradient" onClick={runWorkflow} disabled={isRunning}><Sparkles className="h-4 w-4" />Prepare</Button><Button variant="outline" onClick={() => onSendToGenerator(prompt)}><Send className="h-4 w-4" />Send to Generator</Button></div></div></div></GlassPanel>;
}

export function AIToolsPage({ prompt, setPrompt }: { prompt: string; setPrompt: (prompt: string) => void }) {
  const { toast } = useToast(); const [output, setOutput] = React.useState("Select a tool to transform your current prompt.");
  const tools = [{ name: "Enhance Prompt", icon: Sparkles, run: () => `${prompt}, award-winning cinematography, controlled motion, detailed atmosphere, polished final frame` }, { name: "Remix Style", icon: RefreshCw, run: () => `${prompt}, remixed as futuristic noir with blue ambient light and purple neon accents` }, { name: "Upscale Brief", icon: Wand2, run: () => `Upscale plan: sharpen detail, stabilize motion, improve lighting, export 4K master for: ${prompt}` }, { name: "Variant Pack", icon: Layers, run: () => `Create 4 variants: cinematic, realistic, anime, hyper 3D. Base prompt: ${prompt}` }];
  return <GlassPanel className="p-6 sm:p-8"><PageHeader eyebrow="AI Utilities" title="AI Tools" description="Transform prompts, create variants, upscale briefs, and speed up production decisions." icon={Wand2} /><div className="grid gap-4 lg:grid-cols-4">{tools.map((tool) => <button key={tool.name} onClick={() => { const next = tool.run(); setOutput(next); if (tool.name.includes("Prompt") || tool.name.includes("Remix")) setPrompt(next); toast({ title: `${tool.name} complete`, description: "Tool output is ready." }); }} className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 text-left transition hover:border-violet-300/50 hover:shadow-[0_0_30px_rgba(124,58,237,0.22)]"><tool.icon className="h-6 w-6 text-sky-300" /><h3 className="mt-4 font-semibold text-white">{tool.name}</h3><p className="mt-2 text-sm text-slate-500">Run instantly on the current generator prompt.</p></button>)}</div><div className="mt-5"><FieldLabel label="Tool Output" /><Textarea value={output} onChange={(event) => setOutput(event.target.value)} className="min-h-32" /></div></GlassPanel>;
}

export function BillingPage() {
  const { toast } = useToast(); const [billing, setBilling] = React.useState("monthly"); const [credits, setCredits] = React.useState([120]); const plans = ["Starter", "Pro", "Studio"]; const monthly = [19, 49, 129]; const price = (index: number) => billing === "yearly" ? Math.round(monthly[index] * 12 * 0.8) : monthly[index];
  return <GlassPanel className="p-6 sm:p-8"><PageHeader eyebrow="Workspace Billing" title="Billing" description="Manage plans, credits, billing cycle, and invoices from a single panel." icon={CreditCard} /><div className="mb-5 flex gap-2 rounded-2xl border border-white/10 bg-white/[0.04] p-1 w-fit">{["monthly", "yearly"].map((item) => <button key={item} onClick={() => setBilling(item)} className={`rounded-xl px-4 py-2 text-sm capitalize transition ${billing === item ? "bg-violet-500 text-white" : "text-slate-400 hover:text-white"}`}>{item}{item === "yearly" ? " -20%" : ""}</button>)}</div><div className="grid gap-4 lg:grid-cols-3">{plans.map((plan, index) => <div key={plan} className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><h3 className="text-xl font-semibold text-white">{plan}</h3><p className="mt-2 text-sm text-slate-400">{index === 0 ? "For quick tests" : index === 1 ? "For creators and teams" : "For production studios"}</p><p className="mt-5 text-3xl font-bold text-white">${price(index)}<span className="text-sm text-slate-500">/{billing === "monthly" ? "mo" : "yr"}</span></p>{billing === "yearly" ? <p className="mt-2 text-xs text-emerald-300">Saved ${monthly[index] * 12 - price(index)} annually</p> : null}<ul className="mt-5 space-y-2 text-sm text-slate-300">{["Fast rendering", "Commercial license", `${[200, 900, 3000][index]} credits`].map((feature) => <li key={feature} className="flex gap-2"><Check className="h-4 w-4 text-emerald-300" />{feature}</li>)}</ul><Button className="mt-5 w-full" variant={index === 1 ? "gradient" : "outline"} onClick={() => toast({ title: `${plan} selected`, description: `${billing} billing selected for checkout.` })}>Choose {plan}</Button></div>)}</div><div className="mt-5 rounded-2xl border border-white/10 bg-white/[0.04] p-5"><FieldLabel label="Credit Limit" value={credits[0]} /><Slider value={credits} onValueChange={setCredits} max={1000} step={10} /><Button className="mt-4" variant="outline" onClick={() => toast({ title: "Credit limit updated", description: `${credits[0]} credits limit saved.` })}>Save Credit Limit</Button></div></GlassPanel>;
}

export function SettingsPage() {
  const { toast } = useToast(); const [workspace, setWorkspace] = React.useState("Vidora Studio"); const [autoSave, setAutoSave] = React.useState(true); const [watermark, setWatermark] = React.useState(false); const [email, setEmail] = React.useState(true); const [push, setPush] = React.useState(true); const [privateMode, setPrivateMode] = React.useState(false); const [beta, setBeta] = React.useState(false);
  return <GlassPanel className="p-6 sm:p-8"><PageHeader eyebrow="Preferences" title="Settings" description="Control workspace identity, render defaults, security, notifications, privacy, and export behavior." icon={Settings} /><div className="grid gap-4 lg:grid-cols-2"><div className="space-y-4 rounded-2xl border border-white/10 bg-white/[0.04] p-5"><div><FieldLabel label="Workspace Name" /><Input value={workspace} onChange={(event) => setWorkspace(event.target.value)} /></div><div><FieldLabel label="Default Model" /><Select defaultValue="cinematic"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="cinematic">Cinematic Pro</SelectItem><SelectItem value="realistic">Realistic XL</SelectItem><SelectItem value="anime">Anime Vision</SelectItem></SelectContent></Select></div><div><FieldLabel label="Default Export Quality" /><Select defaultValue="1080p"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="720p">720p</SelectItem><SelectItem value="1080p">1080p</SelectItem><SelectItem value="4k">4K</SelectItem></SelectContent></Select></div><div><FieldLabel label="Team Role" /><Select defaultValue="owner"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="owner">Owner</SelectItem><SelectItem value="editor">Editor</SelectItem><SelectItem value="viewer">Viewer</SelectItem></SelectContent></Select></div><div><FieldLabel label="Security" /><Button variant="outline" onClick={() => toast({ title: "2FA setup started", description: "Security verification flow opened." })}><Lock className="h-4 w-4" />Enable 2FA</Button></div></div><div className="space-y-3 rounded-2xl border border-white/10 bg-white/[0.04] p-5"><ToggleRow label="Auto-save generations" checked={autoSave} onChange={() => setAutoSave(!autoSave)} /><ToggleRow label="Watermark exports" checked={watermark} onChange={() => setWatermark(!watermark)} /><ToggleRow label="Email render updates" checked={email} onChange={() => setEmail(!email)} /><ToggleRow label="Push notifications" checked={push} onChange={() => setPush(!push)} /><ToggleRow label="Private workspace mode" checked={privateMode} onChange={() => setPrivateMode(!privateMode)} /><ToggleRow label="Beta AI models access" checked={beta} onChange={() => setBeta(!beta)} /><Button variant="gradient" onClick={() => toast({ title: "Settings saved", description: `${workspace} preferences updated.` })}><Save className="h-4 w-4" />Save Settings</Button></div></div><div className="mt-4 grid gap-4 md:grid-cols-3"><MiniStat label="Security" value="Good" icon={Shield} /><MiniStat label="Alerts" value={email || push ? "On" : "Off"} icon={Bell} /><MiniStat label="Privacy" value={privateMode ? "Private" : "Team"} icon={Lock} /></div></GlassPanel>;
}

export function SupportPage() {
  const { toast } = useToast(); const [message, setMessage] = React.useState(""); const faqs = [["How long does rendering take?", "Most Vidora previews complete in 2-5 minutes depending on model and resolution."], ["Can I use videos commercially?", "Yes, Pro and Studio plans include commercial usage rights for generated exports."], ["How do credits work?", "Credits are consumed per generation based on duration, model, and output resolution."]]; const socials = [["Instagram", Globe, "@vidora.ai"], ["Twitter", MessageCircle, "@VidoraAI"], ["LinkedIn", Mail, "Vidora AI"], ["YouTube", Film, "Vidora Studio"]];
  return <GlassPanel className="p-6 sm:p-8"><PageHeader eyebrow="Support Desk" title="Help & Support" description="Get answers, submit tickets, contact the team, and follow Vidora AI updates." icon={LifeBuoy} /><div className="grid gap-4 lg:grid-cols-[1fr_0.9fr]"><div className="space-y-4"><Accordion type="single" collapsible className="space-y-3">{faqs.map(([question, answer], index) => <AccordionItem value={`faq-${index}`} key={question}><AccordionTrigger><span className="flex items-center gap-2"><FileQuestion className="h-4 w-4 text-sky-300" />{question}</span></AccordionTrigger><AccordionContent>{answer}</AccordionContent></AccordionItem>)}</Accordion><div className="grid gap-3 md:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><Phone className="mb-3 h-5 w-5 text-sky-300" /><p className="font-semibold text-white">+1 (415) 555-0198</p><p className="text-sm text-slate-400">Mon-Fri, 9 AM - 6 PM</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><Mail className="mb-3 h-5 w-5 text-sky-300" /><p className="font-semibold text-white">support@vidora.ai</p><p className="text-sm text-slate-400">Average reply: 2 hours</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><MapPin className="mb-3 h-5 w-5 text-sky-300" /><p className="font-semibold text-white">San Francisco, CA</p><p className="text-sm text-slate-400">Global remote support</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><Globe className="mb-3 h-5 w-5 text-sky-300" /><p className="font-semibold text-white">help.vidora.ai</p><p className="text-sm text-slate-400">Docs and system status</p></div></div></div><div className="space-y-4"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><FieldLabel label="Contact Support" /><Input className="mb-3" placeholder="Email address" /><Textarea value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Tell us what you need help with" className="min-h-32" /><Button className="mt-4 w-full" variant="gradient" onClick={() => { setMessage(""); toast({ title: "Support ticket sent", description: "Vidora support will reply soon." }); }}><MessageCircle className="h-4 w-4" />Send Ticket</Button></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><h3 className="font-semibold text-white">Social Media</h3><div className="mt-4 grid gap-3">{socials.map(([label, Icon, handle]) => <button key={label as string} onClick={() => toast({ title: `${label} opened`, description: `${handle} copied for quick access.` })} className="flex items-center justify-between rounded-xl border border-white/10 bg-white/[0.04] p-3 text-left text-sm text-slate-200 transition hover:border-violet-300/40"><span className="flex items-center gap-3">{React.createElement(Icon as React.ElementType, { className: "h-4 w-4 text-sky-300" })}{label as string}</span><span className="text-slate-500">{handle as string}</span></button>)}</div></div></div></div></GlassPanel>;
}

